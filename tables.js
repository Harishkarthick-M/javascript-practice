//tables

let i = 1; 
let number = 4; 
while (i <= 12) { 
    let num = number * i;
        console.log(`${number} * ${i} = ${num}`);
    i++; 
}

//odd or even only need to print
// chnage I to i
let I = 1; 
let numb = 4; 
while (i <= 12) { 
    let num = number * I;
    if (I % 2 === 1) {  
        console.log(`${number} * ${I} = ${num}`);
    }
    i++; 
}
